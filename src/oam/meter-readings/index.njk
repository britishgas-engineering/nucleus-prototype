---
layout: nucleus-header-logged-in.njk
title: meter reading journeys
eleventyNavigation:
  key: Meter readings
  parent: oam
  title: Meter readings
  excerpt: Meter reading journeys
---

<ns-landmark type="lakeside">
  <h1 slot="heading">
    <span class="h5">How cool is this.</span>
    <span class="h1 enlighten">{{teamname}}'s <b>{{title}}</b></span>
  </h1>
  <div slot="paragraph">
    <p>This is the index page of <b>{{title}}</b>.</p>
  </div>
</ns-landmark>

  {% set navPages = collections.all | eleventyNavigation("meter-readings") | reverse %}
  <ns-panel>
    <div class="splash">
      <h2>{{teamname}} projects</h2>
    </div>
    <div class="splash triple">
      {%- for entry in navPages %}
        <ns-card type="support">
          <h3 slot="heading">{{entry.title}}</h3>
          <p slot="paragraph">{{entry.excerpt}}</p>
          <a slot="cta" href="{{entry.url | url}}">
            <ns-cta type="text">{{entry.title}}</ns-cta>
          </a>
        </ns-card>
      {%- endfor %}
    </div>
  </ns-panel>

  <ns-caveat>
<h2 slot="heading">Anonymous meter reading credentials</h2>
  <div slot="caveat">
    <p>Details below will activate logic on the prototype for enabled fields. If any alternative information is entered, unhappy paths will be shown.</p>
    <ol>
      <li>Customer's reference number: 851234567890</li>
      <li>Gas meter reading: 51948</li>
      <li>Gas meter reading (Triggers out of tolerance): 53000</li>
      <li>Electricity meter reading: ANY</li>
    </ol>
  </div>
</ns-caveat>
