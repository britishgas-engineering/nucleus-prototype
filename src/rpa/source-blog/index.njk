---
title: Source blog
eleventyNavigation:
  key: Help and Support
  parent: rpa
  title: Source blog
  excerpt: Nucleus redesigns of The Source
---


<ns-landmark type="hillside">
  <h1 slot="heading">
    <span class="h5">Prototypes.</span>
    <span class="h1 enlighten"><b>The Source</b> blog</span>
  </h1>
  <div slot="paragraph">
    <p>This is the index page of <b>{{title}}</b>. When you're ready, move on.</p>
  </div>
</ns-landmark>

{% set navPages = collections.all | eleventyNavigation("source-blog") | reverse %}
  <ns-panel>
    <div class="splash">
      <h2>The Source blog</h2>
    </div>
    <div class="splash triple">
      {%- for entry in navPages %}
        <ns-card type="support">
          <h3 slot="heading">{{entry.title}}</h3>
          <p slot="paragraph">{{entry.excerpt}}</p>
          <a slot="cta" href="{{entry.url | url}}">
            <ns-cta type="text">{{entry.title}}</ns-cta>
          </a>
        </ns-card>
      {%- endfor %}
    </div>
  </ns-panel>


