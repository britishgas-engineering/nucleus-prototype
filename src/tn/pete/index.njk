---
title: Pete's Prototypes
eleventyNavigation:
  key: pete
  parent: tn
  title: Pete's folder
  excerpt: Some prototypes from Pete
---



<ns-landmark type="hillside">
  <h1 slot="heading">
    <span class="h5">Here is.</span>
    <span class="h1 enlighten"><b>Pete's</b> prototypes</span>
  </h1>
  <div slot="paragraph">
    <p>This is the index for what you can find in <b>{{title}}</b>. Keep that in mind and have clemency.</p>
  </div>
</ns-landmark>

  {% set navPages = collections.all | eleventyNavigation("pete") | reverse %}
  <ns-panel>
    <div class="splash">
      <h2>Available projects</h2>
    </div>
    <div class="splash triple">
      {%- for entry in navPages %}
        <ns-card type="support">
          <h3 slot="heading">{{entry.title}}</h3>
          <p slot="paragraph">{{entry.excerpt}}</p>
          <a slot="cta" href="{{entry.url | url}}">
            <ns-cta type="text">Go to {{entry.title}}</ns-cta>
          </a>
        </ns-card>
      {%- endfor %}
    </div>
  </ns-panel>
