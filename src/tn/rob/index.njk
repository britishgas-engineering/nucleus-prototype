---
title: Rob's little corner
eleventyNavigation:
  key: rob
  parent: tn
  title: Rob's folder
  excerpt: This is a where Rob experiments
---



<ns-landmark type="hillside">
  <h1 slot="heading">
    <span class="h5">Sandboxes are the best type of boxes.</span>
    <span class="h1 enlighten"><b>Rob is prototyping</b>, get ready to be amazed</span>
  </h1>
  <div slot="paragraph">
    <p>This is the index for what you can find in <b>{{title}}</b>. Keep that in mind and have mercy on the designer's attempts to code.</p>
  </div>
</ns-landmark>

  {% set navPages = collections.all | eleventyNavigation("ben") | reverse %}
  <ns-panel>
    <div class="splash">
      <h2>Available projects - Why does parent only work if labelled 'Ben'?
      </h2>
    </div>
    <div class="splash triple">
      {%- for entry in navPages %}
        <ns-card type="support">
          <h3 slot="heading">{{entry.title}}</h3>
          <p slot="paragraph">{{entry.excerpt}}</p>
          <a slot="cta" href="{{entry.url | url}}">
            <ns-cta type="text">Go to {{entry.title}}</ns-cta>
          </a>
        </ns-card>
      {%- endfor %}
    </div>
  </ns-panel>
